#pragma once

#include <QObject>

class RemoteAccessManager : public QObject
{
    Q_OBJECT
public:
    explicit RemoteAccessManager(QObject *parent = nullptr);

signals:

public slots:

private:

};
